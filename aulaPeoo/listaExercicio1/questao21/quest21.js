function outraOp(){
    const select = document.getElementById("categoria").selectedIndex;
    console.log(select);
    if (select === 1){
        let outro = document.body.createElement(input, {type="text" id="outrosl" placeholder="Escreva a outra opção"}) =
        
    }
}
